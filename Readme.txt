I'm basically trying to extract the numbers and denominators from a free text field and write the results into a dataframe.

Each text field has a different amount of numbers, and not all numbers have a denominator